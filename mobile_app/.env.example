# Copy to `.env` in this folder (`mobile_app/`). `.env` is gitignored.
#
# Security: `.env` is bundled as a Flutter asset in release builds. Do NOT put API
# secrets, private keys, or admin tokens here — only public URLs and non-secret flags.

# ========== PRODUCTION (Render) — default if APP_API_URL is omitted ==========
# You can omit APP_API_URL entirely; the app defaults to Render.
APP_API_URL=https://orderzhouse-backend.onrender.com
ENV=production

# ========== LOCAL BACKEND ==========
# You MUST set APP_API_URL (ENV=development alone does not switch the API host).
# iOS Simulator / Mac:
# APP_API_URL=http://127.0.0.1:5050
# ENV=development
# Android Emulator:
# APP_API_URL=http://10.0.2.2:5050
# ENV=development
# Physical phone (same Wi‑Fi as dev machine):
# APP_API_URL=http://192.168.x.x:5050
# ENV=development

# Optional: use instead of APP_API_URL
# BASE_URL=https://orderzhouse-backend.onrender.com

# Optional: company subscription survey
# COMPANY_SUBSCRIBE_URL=https://example.com/survey

# Optional: admin WebView base URL (no trailing slash). In-app admin tools load this site inside WebView.
# Defaults to https://orderzhouse.com if unset.
# ADMIN_WEB_URL=https://www.orderzhouse.com

# Show “Pay with PayPal” on subscription screen (clients + freelancers). Add to `mobile_app/.env`, then full restart (not hot reload).
# Backend needs PAYPAL_ENABLED=true + PayPal keys in backendEsModule/.env.
# ENABLE_PAYPAL=true
#
# Debug tip: if APP_API_URL is localhost / 192.168.x.x / 10.0.2.2, the PayPal button also appears without ENABLE_PAYPAL (release builds never).

# Hide PayPal on the subscription sheet (focus on Apple IAP + company). Full restart after change.
# SUBSCRIPTION_HIDE_PAYPAL=true
