// Copy to Secrets.xcconfig (same folder). Never commit Secrets.xcconfig.
//
// From Google Cloud: Credentials → iOS OAuth client → "iOS URL scheme" / Reversed client ID.
// Replace the placeholder value with your real id (letters, digits, dots, hyphens only).

GOOGLE_REVERSED_CLIENT_ID = com.googleusercontent.apps.00000000000000000000
